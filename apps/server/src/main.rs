//! exdb-server — Database server binary
//!
//! Combines the database engine (L6), replication (L7), and wire
//! protocol (L8) into a runnable server process.

fn main() {
    println!("exdb-server: not yet implemented");
}
