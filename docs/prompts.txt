/ralph-loop:ralph-loop "Implement storage engine based on docs/DESIGN.md and following docs/plan/01_storage/*.md, do it in storage package." --completion-promise "DONE" --max-iterations 20

-------------------------------------------------------------------------------------------


/ralph-loop:ralph-loop "Based on DESIGN.md, build an overview implementation plan in docs/plan/overview/*.md.

CRITICAL LAYER BOUNDARY RULE: Before placing a module in a layer, ask: 'Does this module know about domain concepts (documents, MVCC, timestamps, tombstones) or is it a generic data structure operating on bytes/pages?' Generic data structures (B-tree, slotted pages, buffer pool) belong in the storage layer. Domain-aware modules (key encoding, version resolution, document insert/get with MVCC) belong in higher layers.

LAYER 2 INDEPENDENCE TEST: Layer 2 (Storage Engine) MUST be usable as a standalone generic storage library with no document/MVCC knowledge. Its public facade should look like:
  - StorageEngine::open/close — lifecycle
  - StorageEngine::create_btree() → BTreeHandle — allocates a root page, returns a handle
  - StorageEngine::open_btree(root_page) → BTreeHandle — opens existing tree
  - BTreeHandle::get/insert/delete(key: &[u8], value: &[u8]) — raw byte key/value ops
  - BTreeHandle::scan(lower, upper, direction) → ScanIterator — range scan on bytes
  - StorageEngine::heap_store/load/free — large value storage
  - StorageEngine::append_wal(record_type, payload) → Lsn — durable logging
  - StorageEngine::read_wal_from(lsn) → WalIterator — replay
  - StorageEngine::checkpoint() — flush dirty pages, reclaim WAL
  - StorageEngine::recover() — DWB restore + WAL replay
If any method in this facade requires importing a type from Layer 3+ (DocId, Ts, Document, Filter, etc.), the boundary is WRONG. The plan must include this facade in the Layer 2 spec.

The plan must cover:

1. LAYER DEFINITIONS (one file per layer):
   - Layer 1: Core Types & Encoding (DocId, Scalar, FieldPath, Filter, BSON/JSON encoding, ULID)
   - Layer 2: Storage Engine — generic storage primitives with no domain knowledge (page format, buffer pool as page cache, GENERIC B+ tree on slotted pages operating on raw byte keys/values, WAL, heap for large values, free list, DWB, checkpoint, crash recovery, vacuum, catalog B-trees for durable metadata)
   - Layer 3: Document Store & Indexing — adds document/MVCC semantics on top of Layer 2 primitives (PrimaryIndex: insert_version/get_at_ts with MVCC key construction doc_id||inv_ts and tombstone handling and inline-vs-heap decision; SecondaryIndex: scan with version resolution and stale entry skip; order-preserving key encoding; array indexing; background index build). The primary B-tree IS the document store — it is both storage and primary index (clustered). This layer uses Layer 2 BTree but adds all document-aware logic.
   - Layer 4: Query Engine (query planning, index range encoding, scan execution, post-filter, read-your-writes merge, read set interval generation)
   - Layer 5: Transaction Manager (timestamp allocator, read/write sets, OCC validation via commit log, subscription registry + invalidation, single-writer commit protocol)
   - Layer 6: Replication (WAL streaming primary/replica, transaction promotion, 3-tier recovery, snapshot transfer)
   - Layer 7: API/Protocol (frame format with JSON text + binary auto-detect, message types, session state machine, JWT auth, transport listeners)
   - Integration modules: Database instance, SystemDatabase, CatalogCache (in-memory dual-indexed by name AND id), config

2. For EACH layer: list every module, its high-level Rust types (structs + key methods), and a table showing which interfaces it exposes to which higher layers.

3. CATALOG specifically: dual B-tree (by-id primary, by-name secondary) in Layer 2 storage, plus in-memory CatalogCache at integration level. All query interfaces must support both by-name and by-id access for collections, indexes, and databases.

4. HIGH-LEVEL DIAGRAMS (ASCII) for these interaction flows:
   - Document write commit (full 10-step protocol from validate through notify)
   - Point read (get by ID) traversing all layers top to bottom
   - Index scan query pipeline (Source → PostFilter → Terminal)
   - Subscription invalidation on commit
   - WAL replication primary → replica
   - Crash recovery + startup sequence
   - Connection lifecycle (hello → auth → transactions → disconnect)
   - Create collection flow (page allocation → WAL → catalog B-trees → cache)
   - Document insert flow showing Layer 2 vs Layer 3 boundary (BTree.insert bytes vs PrimaryIndex.insert_version with MVCC)

5. IMPLEMENTATION PHASES: bottom-up build order with deliverables and test plan per phase.

For each module placement decision, briefly note WHY it belongs in that layer (one sentence)." --completion-promise "DONE" --max-iterations 20

-------------------------------------------------------------------------------------------


/ralph-loop:ralph-loop "Based on docs/DESIGN.md and docs/plan/overview/*.md (especially 02-layer2-storage-engine.md), create a detailed storage engine implementation plan in docs/plan/storage/*.md.

The storage engine is Layer 2 in the architecture — a GENERIC storage library with NO domain knowledge (no DocId, Ts, Document, Filter, MVCC, or any Layer 3+ types).

REQUIREMENTS:

1. BACKEND ABSTRACTION (do this first):
   - PageStorage trait and WalStorage trait (read/write/sync pages and WAL)
   - FilePageStorage, FileWalStorage — durable filesystem implementations
   - MemoryPageStorage, MemoryWalStorage — ephemeral in-memory implementations
   - StorageEngine::open(path), open_in_memory(), open_with_backend(custom) entry points
   - In-memory backends skip DWB, checkpoint, and recovery (no-ops)

2. SUB-LAYER ORGANIZATION within Layer 2 (bottom-up build order, one file per sub-layer):
   - S1: Backend traits + implementations (PageStorage, WalStorage)
   - S2: Page format (SlottedPage, PageHeader, PageType, checksum)
   - S3: Buffer pool (page cache with clock eviction, SharedPageGuard, ExclusivePageGuard, takes Arc<dyn PageStorage>)
   - S4: Free list (page allocation/deallocation)
   - S5: WAL (WalWriter with group commit, WalReader, segment management, takes Arc<dyn WalStorage>)
   - S6: B+ tree (generic on raw byte keys/values with memcmp ordering, insert/get/delete/scan, split/merge, uses buffer pool)
   - S7: Heap (large value storage with overflow chains)
   - S8: Double-write buffer (torn-write protection, file-backed only)
   - S9: Checkpoint (flush dirty pages through DWB, WAL record)
   - S10: Recovery (DWB restore + WAL replay via WalRecordHandler trait callback)
   - S11: Vacuum (page-level entry removal from B-trees)
   - S12: Catalog B-tree schema (key format for collection/index entries, serialize/deserialize, no MVCC)
   - S13: StorageEngine facade (composes all sub-layers, public API)

3. For EACH sub-layer:
   - Rust types (structs, enums, traits) with method signatures
   - Dependencies on other sub-layers (which sub-layers it uses)
   - Detailed implementation steps (not just 'implement X', but HOW — algorithms, data structures, edge cases)
   - Error handling (what can fail, how to report)
   - Tests (specific test cases with expected behavior)

4. ASCII DIAGRAMS for these internal flows:
   - Page read path: buffer pool miss → PageStorage::read_page → frame → guard
   - Page write path: ExclusivePageGuard drop → mark dirty → checkpoint flush
   - B-tree insert with split: leaf full → split leaf → propagate to parent → possible root split
   - B-tree scan: seek to lower bound → iterate leaf chain → yield entries
   - WAL write path: append request → group commit batching → WalStorage::append → fsync → return LSN
   - Checkpoint flow: snapshot dirty set → DWB write → scatter-write to PageStorage → mark clean → WAL checkpoint record
   - Recovery flow: read meta → DWB recover → open pages → WAL replay from checkpoint_lsn
   - Free list allocate: pop from list or extend file

5. LATCH PROTOCOL:
   - Page table RwLock → Frame RwLock ordering (strict hierarchy)
   - Multiple frame locks in ascending page_id order
   - Never hold frame lock across I/O or await points
   - B-tree latch coupling (crab protocol) for concurrent access

6. PUBLIC FACADE must match overview spec exactly:
   - StorageEngine::open/open_in_memory/open_with_backend/close
   - StorageEngine::create_btree/open_btree → BTreeHandle
   - BTreeHandle::get/insert/delete/scan — raw &[u8] only
   - StorageEngine::heap_store/load/free
   - StorageEngine::append_wal/read_wal_from
   - StorageEngine::checkpoint/recover
   - NO types from Layer 3+ anywhere in the public API

7. IMPLEMENTATION ORDER: specify which sub-layers to build first and test independently before composing. Each sub-layer should be testable in isolation where possible." --completion-promise "DONE" --max-iterations 20

-------------------------------------------------------------------------------------------


/ralph-loop:ralph-loop "Implement the storage engine code (Layer 2) in the storage/ crate based on docs/plan/storage/*.md.

The storage crate is at storage/ and is currently empty (just a main.rs placeholder). Convert it to a library crate (main.rs → lib.rs). All design docs with exact struct definitions, method signatures, algorithms, edge cases, and tests are in docs/plan/storage/01-s1-backend.md through 13-s13-engine-facade.md, plus 14-latch-protocol.md and 15-diagrams.md.

This is a generic storage engine library — NO domain knowledge (no DocId, Ts, Document, Filter, MVCC, or any Layer 3+ types).

IMPLEMENTATION PHASES (build order matters — do them in sequence):

Phase A — Foundations (S1 + S2, no deps between them):
  S1: backend.rs — PageStorage trait, WalStorage trait, MemoryPageStorage, MemoryWalStorage, FilePageStorage, FileWalStorage
  S2: page.rs — PageHeader (zerocopy LE wrappers), PageType enum, SlottedPage, SlottedPageRef, CRC-32C checksum

Phase B — Core Infrastructure (S3 + S4 + S5, depends on Phase A):
  S3: buffer_pool.rs — BufferPool with clock eviction, SharedPageGuard, ExclusivePageGuard, pin_count as AtomicU32 outside Frame RwLock
  S4: free_list.rs — FreeList (LIFO page allocation/deallocation, page 0 never freed)
  S5: wal.rs — WalWriter with group commit (tokio mpsc), WalReader, WalIterator, WAL record type constants (0x01-0x0A)

Phase C — Data Structures (S6 + S7, depends on Phase B):
  S6: btree.rs — B+ tree with memcmp key ordering, insert/get/delete/scan, split/merge, latch coupling (crab protocol)
  S7: heap.rs — Large value storage with overflow page chains

Phase D — Durability (S8 + S9 + S10, depends on Phase B+C):
  S8: dwb.rs — Double-write buffer for torn-write protection (file-backed only, no-op for in-memory)
  S9: checkpoint.rs — Flush dirty pages through DWB, write checkpoint WAL record
  S10: recovery.rs — DWB restore + WAL replay via WalRecordHandler trait callback

Phase E — Maintenance + Catalog (S11 + S12, depends on Phase C):
  S11: vacuum.rs — Page-level entry removal from B-trees
  S12: catalog_btree.rs — Key format for collection/index catalog entries, serialize/deserialize

Phase F — Facade (S13, depends on all):
  S13: engine.rs — StorageEngine struct composing all sub-layers, FileHeader (zerocopy) on page 0

CARGO DEPENDENCIES (add to storage/Cargo.toml):
  zerocopy = { version = \"0.8\", features = [\"derive\"] }
  crc32fast = \"1\"
  parking_lot = \"0.12\"
  tokio = { version = \"1\", features = [\"sync\", \"rt\", \"time\", \"io-util\"] }
  thiserror = \"2\"
  [dev-dependencies]
  tempfile = \"3\"
  tokio = { version = \"1\", features = [\"full\", \"test-util\"] }

PLEASE USE EFFICIENT CONCURRENCY MODEL, USE ASYNC AND TOKIO

CRITICAL DESIGN CONSTRAINTS:
1. All on-disk formats use little-endian. PageHeader and FileHeader use zerocopy LE wrapper types.
2. Latch hierarchy (strict): component Mutex (free_list/heap/file_header) → page_table RwLock → frame RwLock. Never reverse.
3. Multiple frame locks: always ascending page_id order.
4. B-tree uses latch coupling (crab protocol) for concurrent access.
5. pin_count is AtomicU32 OUTSIDE the Frame RwLock (both guard types can decrement on Drop).
6. No frame locks held across I/O or await points.
7. Page 0 is the FileHeader page — never freed, never evicted.
8. Recovery is internal to open() — no public recover() method.
9. In-memory backends skip DWB, checkpoint scatter-write, and recovery (all no-ops).

FOR EACH PHASE: read the corresponding docs/plan/storage/XX-*.md file(s) thoroughly BEFORE writing code. They contain the exact types, algorithms, and test cases. Write all tests listed in the plan. Make sure cargo test passes before moving to the next phase." --completion-promise "DONE" --max-iterations 20
